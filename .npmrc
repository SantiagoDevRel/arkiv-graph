# pnpm + Next.js (Turbopack) on Windows needs a hoisted node_modules layout,
# otherwise the workspace lib + react-force-graph fail to resolve at build time.
node-linker=hoisted
shamefully-hoist=true
