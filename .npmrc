# pnpm + Next.js (Turbopack) on Windows needs a hoisted node_modules layout,
# otherwise react-force-graph can fail to resolve at build time.
node-linker=hoisted
shamefully-hoist=true
link-workspace-packages=false
